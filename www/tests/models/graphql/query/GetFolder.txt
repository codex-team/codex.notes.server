GetFolder ($id: ID!, $ownerId: ID!){
  folder (id: $id, ownerId: $ownerId) {
      id,
      owner {
        id
      },
      title,
      dtCreate,
      dtModify,
      isShared,
      isRemoved
    }
}