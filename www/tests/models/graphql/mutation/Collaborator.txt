Collaborator($userId: ID!, $ownerId: ID!, $folderId: ID!, $email: String!, $dtInvite:Int!, $isRemoved: Boolean!) {
  collaborator(userId: $userId, ownerId: $ownerId, folderId: $folderId, email: $email, dtInvite: $dtInvite, isRemoved: $isRemoved) {
    user {
        id
    },
    email,
    dtInvite,
    isRemoved
  }
}